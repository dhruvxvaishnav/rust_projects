[package]
name = "rusty-llm"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
memmap2 = "0.9"
tokenizers = { version = "0.20", default-features = false, features = ["onig"] }
rand = "0.8"

[dev-dependencies]
approx = "0.5"
